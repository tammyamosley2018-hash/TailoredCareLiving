/* Centralized GitHub Pages deployment configuration.
   Create a private form with Formspree, Basin, Formspark, or a comparable
   server-side form service and paste its HTTPS endpoint below. */
window.TCL_CONFIG = Object.freeze({
  applicationEndpoint: '',
  businessEmail: '',
  businessPhone: ''
});
